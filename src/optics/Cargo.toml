[package]
name    = "enso-optics"
version = "0.1.1"
authors = ["Enso Team <contact@luna-lang.org>"]
edition = "2018"

[lib]

[features]

[dependencies]
enso-prelude = { version = "0.1.1", path = "../prelude" }
